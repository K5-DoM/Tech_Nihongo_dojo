{
  "name": "tech-nihongo-dojo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "test": "pnpm --filter shared test && pnpm --filter api test",
    "build": "pnpm -r run build",
    "deploy": "pnpm --filter api deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.28.2"
}
